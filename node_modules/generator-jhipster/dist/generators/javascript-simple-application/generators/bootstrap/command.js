const command = {
    configs: {
        packageJsonNodeEngine: {
            cli: {
                // Accepts a boolean or a string
                type: (val) => val,
                hide: true,
            },
            scope: 'storage',
        },
    },
    import: [],
};
export default command;
