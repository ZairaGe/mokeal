import type { ValidationResult } from '../../base-core/api.d.ts';
/**
 * Generate a KeyStore.
 */
export declare function generateKeyStore(keyStoreFile: string, { packageName }: {
    packageName: string;
}): Promise<ValidationResult>;
