import type CoreGenerator from '../../base-core/generator.ts';
import type { Entity as BaseApplicationEntity, Field as BaseApplicationField } from '../types.ts';
export declare function prepareCommonFieldForTemplates(entityWithConfig: BaseApplicationEntity, field: BaseApplicationField, generator: CoreGenerator): BaseApplicationField;
/**
 * From an enum's values (with or without custom values), returns the enum's values without custom values.
 * @param {String} [enumValues] - an enum's values.
 * @return {Array<String>} the formatted enum's values.
 */
export declare function getEnumValuesWithCustomValues(enumValues: string): {
    name: string;
    value: string;
}[];
