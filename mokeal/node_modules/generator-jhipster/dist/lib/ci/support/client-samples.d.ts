import type { ConfigAll } from '../../types/command-all.ts';
import { type Matrix } from './matrix-utils.ts';
export declare const buildClientSamples: (commonConfig?: ConfigAll) => Matrix;
export declare const clientSamples: Matrix;
